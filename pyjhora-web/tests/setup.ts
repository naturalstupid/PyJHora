import '@testing-library/jest-dom/vitest'

// Global test utilities - using Node environment for core calculation tests
// jsdom will be configured per-file for React component tests

beforeAll(() => {
  // Setup any global mocks
})

afterEach(() => {
  // Cleanup after each test
})
